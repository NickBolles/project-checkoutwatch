/**
 * Contact addresses published on the unauthenticated pages and in the App Store
 * listing. They live here rather than inline so the domain can be changed in one
 * place -- the pending rebrand (docs/PROGRESS.md) and the move to the
 * nickbolles.com host both change them.
 *
 * ⚠️ Every address below must be a mailbox someone actually reads before the
 * listing is submitted. Shopify contacts the support address during review, and
 * an unrouted address is a rejection cause on its own.
 */
export const SUPPORT_EMAIL = "support@checkoutwatch.app";
export const PRIVACY_EMAIL = "privacy@checkoutwatch.app";

/** Business hours quoted on /support, in the timezone support is staffed from. */
export const SUPPORT_HOURS = "Monday to Friday, 9:00–17:00 UTC";

/** Target first-response time quoted on /support and in the listing. */
export const SUPPORT_RESPONSE_TARGET = "one business day";
